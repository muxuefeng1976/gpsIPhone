import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should have GPS Tracker title', (WidgetTester tester) async {
    // Basic smoke test
    expect(true, isTrue);
  });
}
